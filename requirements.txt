# NÃO liste torch/torchvision aqui.
# A imagem GPU já traz o build cu128 (sm_120) e a imagem dev traz o build CPU.
# Se o pip reinstalar torch por dependência, a RTX 5070 para de funcionar.

ultralytics>=8.4.0        # >=8.4 para YOLO26; YOLO11/YOLOv10 funcionam desde 8.3
opencv-python-headless>=4.10
numpy>=1.26,<3
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
pillow>=10.4
scipy>=1.14
gdown>=5.2
