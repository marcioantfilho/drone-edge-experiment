# NÃO liste torch/torchvision aqui.
# A imagem GPU já traz o build cu128 (sm_120) e a imagem dev traz o build CPU.
# Se o pip reinstalar torch por dependência, a RTX 5070 para de funcionar.

# Fixado porque definições internas de métrica/validator afetam o resultado.
# O log de proveniência registra o restante do ambiente resolvido pelo Docker.
ultralytics==8.4.140
opencv-python-headless>=4.10
numpy>=1.26,<3
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
pillow>=10.4
scipy>=1.14
gdown>=5.2
